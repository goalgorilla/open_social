social_album.post:
  path: '/node/{node}/post'
  defaults:
    _entity_form: 'post.album'
    _title: 'Add images to album'
    type: photo
  requirements:
    _permission: 'add post entities'
    node: \d+
  options:
    parameters:
      node:
        type: 'entity:node'
