extend type User {
    """
    The number of private message that the user has sent on the platform.
    This only counts private messages that still exist on the platform.
    """
    privateMessageSent: Int!
}
