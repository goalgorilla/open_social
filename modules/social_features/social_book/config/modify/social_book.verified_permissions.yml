dependencies:
  config:
    - node.type.book
items:
  user.role.verified:
    expected_config:
      permissions: { }
    update_actions:
      add:
        permissions:
          - 'view node.book.field_content_visibility:community content'
