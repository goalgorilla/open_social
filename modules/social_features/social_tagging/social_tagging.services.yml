services:
  social_tagging.manager:
    class: Drupal\social_tagging\TaggingManager
    arguments: ['@entity_field.manager', '@entity_type.manager']

  social_tagging.translation_defaults:
    class: Drupal\social_tagging\ContentTranslationDefaultsConfigOverride
    arguments: ['@module_handler']
    tags:
      - { name: config.factory.override, priority: 5 }
