views.view.event_manage_enrollment_invites:
  expected_config: { }
  update_actions:
    delete:
      display:
        default:
          display_options:
            filters:
              langcode: { }
