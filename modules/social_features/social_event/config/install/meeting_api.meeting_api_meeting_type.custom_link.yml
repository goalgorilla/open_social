langcode: en
status: true
dependencies:
  config:
    - meeting_api.meeting_api_server.manual_url
id: custom_link
label: 'Join link'
description: 'The meeting will take place via a third-party service remote link (like Google Meet, Zoom, etc.).'
server_id: manual_url
