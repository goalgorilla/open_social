# Schema for the configuration files of the ConfigLanguageManager Test module.
social_core_test.system:
  type: config_object
  label: 'Configuration system'
  mapping:
    foo:
      type: string
      label: 'Foo'
