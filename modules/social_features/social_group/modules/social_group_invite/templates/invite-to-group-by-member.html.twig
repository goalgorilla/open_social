{#
/**
 * @file
 * Default theme implementation for main invite_to_group_by_member theme.
 *
 * Available variables:
 * - url: Url that should be copied.
 * - link: \Drupal\Core\Link.
 *
 * @ingroup themeable
 */
#}
{{ attach_library('social_group_invite/share-group-link') }}

{% if url %}
  {{ 'Copy URL'|t }}
  <div class="input-group" id="group-link-share">
    <input type="text" value="{{ url }}" class="form-control" readonly>
    <div class="input-group-addon">
      <span class="far fa-copy"></span>
    </div>
  </div>
{% endif %}
{{ link }}
