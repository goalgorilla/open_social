dependencies:
  module:
    - gaia_vector_index
    - social_topic
    - search_api_attachments
  config:
    - field.field.node.topic.field_files
    - node.type.topic
    - search_api.index.attachments
items:
  search_api.index.attachments:
    expected_config: { }
    update_actions:
      add:
        datasource_settings:
          'entity:node':
            bundles:
              selected:
                - topic
