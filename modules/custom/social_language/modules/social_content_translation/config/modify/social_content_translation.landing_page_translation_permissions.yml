dependencies:
  module:
    - content_translation
    - social_content_translation
    - social_landing_page
items:
  user.role.contentmanager:
    expected_config:
      permissions: { }
    update_actions:
      add:
        permissions:
          - 'translate landing_page node'
  user.role.sitemanager:
    expected_config:
      permissions: { }
    update_actions:
      add:
        permissions:
          - 'translate landing_page node'
