name: GraphQL API
description: Enables the GraphQL API for Open Social.
# This is disabled for new platforms as it's experimental. When Open Social
# defaults to API-first and this module is no longer experimental this can be
# marked as a default optional (or even required) module.
default: false
