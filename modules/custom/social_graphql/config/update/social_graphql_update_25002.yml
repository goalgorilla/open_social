# Disable GraphQL introspection on the Open Social GraphQL server (PROD-34935).
graphql.graphql_servers.open_social_graphql:
  expected_config: {}
  update_actions:
    change:
      disable_introspection: true
