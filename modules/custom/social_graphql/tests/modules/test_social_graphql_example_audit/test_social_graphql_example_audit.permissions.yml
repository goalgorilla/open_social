execute open_social_graphql arbitrary graphql requests:
  title: 'Dummy permission'
  description: "Exists so that GraphqlAuditSubscriberTest can work without installing the config from social_graphql, since we don't want the Open Social GraphQL server in this test but our custom one."
