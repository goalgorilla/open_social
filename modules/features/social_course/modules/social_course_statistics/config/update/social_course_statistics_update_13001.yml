views.view.course_statistics:
  expected_config: { }
  update_actions:
    change:
      display:
        default:
          display_options:
            access:
              type: group_permission
              options:
                group_permission: 'view course statistics'

views.view.course_sections_statistics:
  expected_config: { }
  update_actions:
    change:
      display:
        default:
          display_options:
            access:
              type: group_permission
              options:
                group_permission: 'view course statistics'
