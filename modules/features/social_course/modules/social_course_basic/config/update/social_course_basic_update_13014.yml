group.role.course_basic-member:
  expected_config: { }
  update_actions:
    add:
      third_party_settings:
        social_group:
          hubs_filter: true
          full_label: 'Basic Course member'
group.role.course_basic-group_manager:
  expected_config: { }
  update_actions:
    add:
      third_party_settings:
        social_group:
          hubs_filter: true
          full_label: 'Basic Course leader'
