items:
  views.view.group_per_type:
    expected_config: { }
    update_actions:
      add:
        display:
          course_group:
            display_options:
              filters:
                type:
                  value:
                    course_basic: course_basic
                type_1:
                  value:
                    course_basic: course_basic
