translation_patterns:
  - matches: 'message.template.*.yml'
    translatable_keys:
      - key: label
        context: 'Message template label'
      - key: description
        context: 'Message template description'
      - key: value
        context: 'Message template text'
