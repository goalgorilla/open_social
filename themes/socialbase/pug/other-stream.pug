extends ./layout/base-lu.pug

block title
  title Jenny Baker

block pagetitle
  .container.hero-container
    include ./molecules/_other-profile-hero.pug

    .brand-secondary.z-depth-1
      nav(role="navigation" aria-label="Tabs")
        h2.visually-hidden Secondary navigation
        ul.nav.nav-tabs.tabs-center.brand-secondary.nav-tabs--xl
          li.active
            a(href="other-stream.html") Stream
            span.visually-hidden (active tab)
          li
            a(href="other-profile-about.html") About
          li
            a(href='other-topics.html') Topics
          li
            a(href='other-events.html') Events
          li
            a(href="other-groups.html") Groups

block content

  .row.margin-top-l

    .col-xs-12.col-sm-4.col-sm-last.margin-top-complementary
      include ./molecules/_other-events.pug
      include ./molecules/_other-topics.pug
      include ./molecules/_other-groups.pug

    .col-xs-12.col-sm-8
      h4.section-title.section-title-stream Jenny Baker`s stream

      ul.stream
        li.stream-item
          i.stream-icon
          .card.stream-card
            .card-body
              include ./molecules/_stream-post-form.pug
        li.stream-item
          i.stream-icon
          .card.stream-card
            .card-body
              include ./molecules/_stream-post2.pug
        li.stream-item
          i.stream-icon
          .card.stream-card
            .card-body
              include ./molecules/_stream-post2.pug

        li.stream-item
          i.stream-icon
          .card.stream-card
            .card-body
              include ./molecules/_stream-post2.pug

        li.stream-item
          i.stream-icon
          .card.stream-card
            .card-body
              include ./molecules/_stream-post2.pug

block scriptinline

  script.
    function showMe (it, radio) {
      var visibility = radio.value;
      var icon = jQuery(radio).prevAll('svg').html();
      jQuery('#' + it).find('.text').html(visibility);
      jQuery('#' + it).find('svg').html(icon);
      jQuery('#' + it).next('.dropdown-menu').find('.list-group-item').removeClass('active');
      jQuery(radio).closest('.list-group-item').addClass('active');
    }
