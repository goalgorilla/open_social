{#
/**
 * @file
 * Default theme implementation for confirm form.
 *
 * By default this does not alter the appearance of a form at all,
 * but is provided as a convenience for themers.
 *
 * Available variables:
 * - form: The confirm form.
 *
 * @ingroup themeable
 */
#}

<div class="card">
  <div class="card-body">
    {{ form }}

    {#
      We have closed the card element in another template, so the buttons are under the card

      Related files:
      1. container--close-card.html.twig
    #}
